exports.processAI = async (input) => {
  // Simulated AI logic (replace with real model)
  return {
    original: input,
    summary: input.slice(0, 50),
    sentiment: "positive",
    confidence: Math.random().toFixed(2)
  };
};
